<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @post.description %>
</p>
<% if user_signed_in? && current_user.id == @post&.user&.id %>
<%= link_to 'Edit', edit_post_path(@post) %> |
<% end %>


<%= link_to 'comments',comments_post_path  %>
<%= render 'events/comments',comments: @comments %>

<% if user_signed_in? && current_user.id == @post.user.id %>
  <%= render 'events/new_comment',obj: @post %>
<% end %>
<br>
<%= link_to 'Back', posts_path %>